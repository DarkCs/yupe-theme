mixin product-price($price, $old)
    .product-price&attributes(attributes)
        = $price
        +ruble()
    if $old
        .product-price.product-price_old
            = $old
            +ruble()
